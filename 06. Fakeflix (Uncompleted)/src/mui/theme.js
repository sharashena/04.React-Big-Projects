import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    globalText: {
      fontSize: "1.15rem",
      fontFamily: "arial",
      letterSpacing: "2px",
      textTransform: "capitalize",
    },
  },
});
